# Core ML stack
torch>=2.2,<3.0
torchvision>=0.17,<1.0
snntorch>=0.9.0
numpy>=1.26,<3.0

# Config + data
pyyaml>=6.0

# Experiment tracking
wandb>=0.16
tensorboard>=2.15

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Reinforcement learning (used from Phase 0 L4 onward)
gymnasium>=0.29

# Notebook + dev convenience
jupyter>=1.0
ipykernel>=6.29
pytest>=8.0

# The project itself, plus the dashboard/server extra.
#
# WHY THIS LINE EXISTS. Without it a fresh checkout cannot even COLLECT `tests/server`: those
# tests import `fastapi` and `starlette.testclient`, and a collection error fails the WHOLE
# pytest run rather than just those 7 tests. The same line installs `neuromorphic` editable,
# which every other test needs to import at all.
#
# It references pyproject's `[project.optional-dependencies] server` rather than restating
# fastapi/uvicorn/httpx here, so the two files cannot drift apart.
-e .[server]
