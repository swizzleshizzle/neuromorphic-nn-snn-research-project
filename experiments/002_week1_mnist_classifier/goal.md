NEUROMORPHIC PROJECT — Phase 0, Module L1 (Hands-On)

Your first coding session. Build a basic MNIST handwritten digit classifier:
1. Load the MNIST dataset via torchvision
2. Build a simple 2-layer feedforward network
3. Train it for 5-10 epochs
4. Evaluate accuracy (target: 95%+)
5. Modify the architecture (add a layer, change sizes) and observe what happens

This is the 'hello world' you'll compare against when you build the spiking version in Phase 1.

Use Claude to help with any code issues — don't stay stuck more than 15 minutes.