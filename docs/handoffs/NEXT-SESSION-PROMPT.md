# Prompt for a fresh session

Paste the block below into a new session. It is deliberately short - the handoff carries the
detail, and duplicating it here would create two versions that drift.

---

```
Picking up the neuromorphic cube project. NOTHING IS RUNNING, the laptop is FREE, main is
clean at 04471c7, suite 714 tests (692 not slow) plus 88 vitest + 2 e2e in dashboard/.
Phase 3 is CHECKPOINTED and tagged phase-3-checkpoint. EXP-064/065/066 are done and banked,
and the five-region TOPOLOGY line is now closed negative.

Read docs/handoffs/SESSION-HANDOFF-2026-09-21.md first, then CLAUDE.md. Ignore earlier handoffs.

THERE IS NO REQUIRED NEXT TASK. Weeks 24 and 25 are both complete, so do not invent a
deliverable for them. Section 4 lists what is open and four things NOT to do - in
particular, do not re-run the topology question with a different hyperparameter.
Phase 4 (documentation) starts Oct 5 and is laptop-free.

Ask me what I want to work on before starting anything.
```
